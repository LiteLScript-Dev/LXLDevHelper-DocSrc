/**
 * 获取所有数据项名字
 */ 
class DB {
  
  
  


/**
 * 写入数据项
 * @param {string} name 
 * @param {any} data 
 * @returns {boolean} 
 */
 set(name,data)

/**
 * 读取数据项
 * @param {string} name 
 * @returns {any} 
 */
 get(name)

/**
 * 删除数据项
 * @param {string} name 
 * @returns {boolean} 
 */
 delete(name)

/**
 * 获取所有数据项名字
 * @returns {string[]} 
 */
 listKey()

/**
 * 关闭数据库
 * @returns {} 是否成功关闭
 */
 close()  



}
